# Futures Cone

Use a futures cone to distinguish three ranges around a stated present condition:
plausible futures shaped by current drivers, possible futures that become available if
constraints change, and preferable futures that depend on explicit values and action.

The output is a comparative frame for choosing a direction, not a prediction. Keep the
assumptions that constrain or widen each range visible, and identify who defines what
counts as preferable.
